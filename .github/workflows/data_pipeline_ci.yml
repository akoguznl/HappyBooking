name: HappyBooking Data Pipeline CI/CD

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Python 3.9
        uses: actions/setup-python@v5
        with:
          python-version: '3.9'
          cache: 'pip'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pandas pyspark great_expectations pytest

      - name: Python Syntax Check
        run: |
          echo "✍️ Kod yapısı kontrol ediliyor..."
          python -m compileall .

      - name: Run Data Quality Tests
        run: |
          echo "🔍 Great Expectations testleri başlatılıyor..."
          # Buraya gerçek test komutlarını ekleyebilirsin
          
      - name: Slack Notification
        if: always()
        uses: rtCamp/action-slack-notify@v2
        env:
          SLACK_WEBHOOK: ${{ secrets.SLACK_WEBHOOK_URL }}
          SLACK_COLOR: ${{ job.status == 'success' ? 'good' : 'danger' }}
          SLACK_TITLE: "🚀 HappyBooking CI Status: ${{ job.status }}"
          # Mesajı tek satıra indirgeyerek veya düzgün blok kullanarak hatayı giderdik:
          SLACK_MESSAGE: "Commit: ${{ github.event.head_commit.message }}\nAuthor: ${{ github.actor }}\nStatus: ${{ job.status }}\nLink: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}"
          SLACK_ICON: https://github.com/microsoft.png
          SLACK_USERNAME: "Fabric-CI-Bot"
